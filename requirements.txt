# Core
pyyaml>=6.0          # config file loading/saving

# PLC communication (EtherNet/IP for Allen Bradley CompactLogix/ControlLogix)
pylogix>=1.0

# GUI (industrial HMI)
PySide6>=6.5

# Serial (virtual COM port) scanners, e.g. Zebra in USB-CDC mode
pyserial>=3.5
