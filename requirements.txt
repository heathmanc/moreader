# Core
pyyaml>=6.0          # config file loading/saving

# PLC communication (EtherNet/IP for Allen Bradley CompactLogix/ControlLogix)
pylogix>=1.0

# Optional: only needed when scanner.type == "serial"
pyserial>=3.5

# GUI uses Tkinter, which ships with CPython (on Linux install the OS package
# python3-tk).  No pip dependency required.
